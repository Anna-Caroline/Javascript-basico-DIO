// importar com require
// quando a gente importa algo, a gente importa o que está sendo exportado
const {gets, print} = require('./aula6');

print(gets());
//console.log(funcoes.gets());

//tem como destruir um objeto {}
